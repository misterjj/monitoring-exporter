app = {
  routes = {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  },
  youtrack = {
    host = "localhost"
    host = ${?YOUTRACK_HOST}

    token = "not defined"
    token = ${?YOUTRACK_TOKEN}
  },
  gitlab = {
    host = "localhost"
    host = ${?GITLAB_HOST}

    token = "not defined"
    token = ${?GITLAB_TOKEN}
  }
}
